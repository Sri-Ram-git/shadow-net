[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000} --workers 1 --proxy-headers"
healthcheckPath = "/api/health/live"
healthcheckTimeout = 30

[service]
name = "shadownet-api"