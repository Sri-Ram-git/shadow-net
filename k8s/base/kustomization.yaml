apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: shadownet

resources:
  - namespace.yaml
  - configmap.yaml
  - secret.yaml
  - pv.yaml
  - deployment.yaml
  - service.yaml
  - hpa.yaml
  - ingress.yaml

commonLabels:
  app.kubernetes.io/managed-by: kustomize
  app.kubernetes.io/part-of: shadownet
